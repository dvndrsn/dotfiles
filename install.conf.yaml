- defaults:
    link:
      create: true
      relink: true

- clean: ['~', '~/.config']

- link:
    ~/.bash:
    ~/.bash_profile:
    ~/.bashrc:
    ~/.emacs.d:
    ~/.iterm:
    ~/.spacemacs:
    ~/.gitconfig:
    ~/.gitignore_global:
    #~.pythonrc:
    ~/.vim:
    
# - shell:
#    - mac_setup.sh 
