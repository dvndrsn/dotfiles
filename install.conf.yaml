- defaults:
    link:
      create: true
      relink: true

- clean: ['~', '~/.config']

- link:
    ~/.atom:
    ~/.bash:
    ~/.bash_profile:
    ~/.bashrc:
    ~/.emacs.d:
    ~/.iterm:
    ~/.spacemacs:
    ~/.gitconfig:
    ~/.gitignore_global:
    ~/.vim:
    ~/.vscode:
    
